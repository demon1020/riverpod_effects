name: riverpod_effects
description: One-time side effects (navigation, snackbars, dialogs) for Riverpod.
version: 0.0.1
repository: https://github.com/yourusername/riverpod_effects

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

topics:
  - riverpod
  - side-effects
  - state-management
  - flutter

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  riverpod: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
